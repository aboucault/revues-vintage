---
import DaterUnPatronPage from '../../components/pages/DaterUnPatronPage.astro'
import { fetchPochettesPatron } from '../../lib/sanity'
import { buildCatalog } from '../../lib/catalog'

const pochettes = await fetchPochettesPatron('fr')
const entries = buildCatalog([], pochettes, [])
---
<DaterUnPatronPage locale="fr" entries={entries} />
